/**
 * Re-export from context to maintain backward compatibility
 * All components should now share the same config instance
 */
export { useAppConfig, type AppConfig } from '../contexts/AppConfigContext';

